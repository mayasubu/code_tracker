{
  "name": "codetrack",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:admin": "npm install --prefix admin-portal && npm run build --prefix admin-portal",
    "build:student": "npm install --prefix student-portal && npm run build --prefix student-portal",
    "build": "npm run build:admin && npm run build:student",
    "start": "node app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "ejs": "^5.0.1",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "mongoose": "^9.3.2"
  }
}
